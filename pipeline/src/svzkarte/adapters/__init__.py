"""Länder-Adapter: je Land `normalize() -> GeoDataFrame` (kanonisches Schema).

Gemeinsame Holer/Mapper liegen in `base.py`; die Registry (`registry.py`) bildet
land-code -> Adapter ab. Ein neuer Adapter = neue Datei + Eintrag in der Registry.
"""
